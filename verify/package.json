{
  "name": "verify-cli",
  "version": "0.1.0",
  "description": "Ask an LLM whether an input follows a set of instructions; the exit code is the yes/no answer.",
  "bin": { "verify": "bin/verify" },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
