[package]
name = "analytics-fn"
version = "0.1.0"
edition = "2021"
description = "Gatekeeper function: aggregated website-visit analytics from datalog-db (PageView)."

[lib]
crate-type = ["cdylib"]

[dependencies]
gatekeeper-fn = { path = "../../crates/gatekeeper-fn" }
datalog-client = { path = "../../../axiom-reload/crates/datalog-client" }
serde_json = "1"
