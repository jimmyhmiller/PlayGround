[package]
name = "gatekeeper-fn-macro"
version = "0.1.0"
edition = "2021"
description = "The #[handler] attribute macro for gatekeeper-fn. Not used directly; re-exported by gatekeeper-fn."

[lib]
proc-macro = true

[dependencies]
