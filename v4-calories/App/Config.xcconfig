// Build configuration for CumulativeTracker.
// Real secrets live in Config.local.xcconfig, which is gitignored.
// Copy Config.local.example.xcconfig -> Config.local.xcconfig and fill it in.

DEEPSEEK_API_KEY =

#include? "Config.local.xcconfig"
