# shellcheck shell=sh
# With the rocm7.2 PyTorch wheels pinned in pyproject.toml, gfx1151 (Strix
# Halo) is supported natively — no env override needed.
#
# This file remains as a placeholder for future ROCm versions or fallback
# to older wheels. Sourcing it is currently a no-op.
:
