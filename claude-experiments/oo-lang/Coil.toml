[package]
name  = "scry"
entry = "src/main.coil"
[link]
# Phase 8a: real HTTP(S) client via libcurl (system /usr/lib/libcurl.4.dylib, 8.7.1).
# `-lcurl` resolves against the default linker paths; no extra -L needed on macOS.
libs = ["curl"]
