enum Shape {
  Circle(Float)
  Rect(Float, Float)
  Dot
}
fn area(s: Shape) -> Float {
  match s {
    Circle(r) -> 3.14 * r * r
    Rect(w, h) -> w * h
    Dot -> 0.0
  }
}
fn main() {
  print(area(Circle(2.0)))
  print(area(Rect(3.0, 4.0)))
  print(area(Dot))
}
