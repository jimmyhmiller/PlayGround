[package]
name = "glaze"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Glaze — a staged style/shader language. Pure compiler (no Bevy)."

[dependencies]

[dev-dependencies]
