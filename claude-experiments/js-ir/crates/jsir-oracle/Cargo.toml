[package]
name = "jsir-oracle"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Test/dev harness: loads upstream golden fixtures (FileCheck form), reconstructs
# byte-exact expected output, and byte-diffs our pipeline against them. Also a thin
# shim to the upstream C++ `jsir_gen` binary when it has been built.

[dependencies]

[dev-dependencies]
