[package]
name = "ion-layout"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
description = "Rust-backed Graphviz layout plugin; layered layout ported from iongraph (Ben Visness, MPL-2.0)"

[lib]
name = "ion_layout"
crate-type = ["staticlib", "rlib"]

[profile.release]
lto = true
codegen-units = 1
