fn pick<T>(c: bool, a: T, b: T) -> T { if c { a } else { b } }
fn first<T>(v: Vec<T>) -> i64 { vec_get_unchecked(v, 0) }
fn main() -> i64 { let mut v = vec_new(); v = vec_push(v, 42); pick(true, first(v), 0) }
