fn main() -> i64 {
    let c = 3.5 * 2.0 + 1.0;          // 8.0
    let s = sqrt(c + 8.0);            // 4.0
    let g = if 1.5 < 2.5 { 1 } else { 0 };
    float_to_int(c) + float_to_int(s) + g + 29
}
