fn main() -> i64 {
    let t = (40, 2);
    let a = [10, 20, 12];
    let r = [7; 3];
    let s1 = t.0 + t.1;             // 42
    let s2 = a[0] + a[1] + a[2];    // 42
    let s3 = r[0] + r[1] + r[2];    // 21
    s1 + s2 - s3 - 21               // 42 + 42 - 21 - 21 = 42
}
