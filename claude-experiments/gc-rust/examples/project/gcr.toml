# A gc-rust project manifest. `gcr run examples/project` reads this and builds
# the entry file. (Run from the repo root: `gcr run examples/project`.)
[package]
name = "calculator"
version = "0.1.0"
entry = "src/main.gcr"
