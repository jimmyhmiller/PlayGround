[package]
name = "structured-lang"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
eframe = "0.31"

[dev-dependencies]
proptest = "1"
