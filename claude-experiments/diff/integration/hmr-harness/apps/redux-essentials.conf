# Real OSS app: reduxjs/redux-essentials-example-app (~1000 modules, RTK,
# vite.config.mts, @vitejs/plugin-react v4). Cloned + installed on demand.
# The edited component is a NAMED export (`export const Navbar`) — the exact shape
# whose live-binding getter exports tripped react-refresh v4 into a full reload
# before the native transform + getter-copy fix. STATE=preserve gates that fix.
NAME=redux-essentials
REPO=https://github.com/reduxjs/redux-essentials-example-app.git
COMMIT=b4414e1504c914ece3253dd8a21adfb2278464d9
INSTALL="npm install --no-audit --no-fund"
EDIT_FILE=src/components/Navbar.tsx
FIND="Redux Essentials Example"
REPLACE="Redux Essentials HMR"
MOUNT_TEXT="Redux Essentials Example"
STATE=preserve
