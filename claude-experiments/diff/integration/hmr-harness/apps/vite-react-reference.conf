# Offline smoke: the in-repo Vite SPA fixture (already installed, no network).
# Proves the harness itself works and Fast Refresh preserves state on a
# default-export component. @vitejs/plugin-react v6.
NAME=vite-react-reference
LOCAL_DIR=integration/vite-react-reference
EDIT_FILE=src/App.tsx
FIND="Get started"
REPLACE="Get started HMR"
STATE=preserve
