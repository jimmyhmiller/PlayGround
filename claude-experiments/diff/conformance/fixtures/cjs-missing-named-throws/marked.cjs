// A CommonJS module that stamps the `__esModule` convention marker on itself.
// The marker is NOT proof of an ES module (tslib and every `importHelpers`
// build stamp it), so the named exports this module actually provides are
// exactly its own `exports` keys — `missingName` is not one of them.
exports.__esModule = true;
exports.present = "present-val";
