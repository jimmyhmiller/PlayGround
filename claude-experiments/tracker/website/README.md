# Ease landing site

Plain HTML + one CSS file. No build step. Drop this folder onto Netlify, Cloudflare Pages, GitHub Pages, or any static host.

## To finish before going live

- Replace `LEMON_SQUEEZY_CHECKOUT_URL` in `index.html` with the real checkout link.
- Replace `idREPLACEME` in the App Store button URL once the app is approved.
- Replace `support@example.com` in `privacy.html` and `support.html` with the real email.
- Drop a screenshot at `assets/hero.png` (recommended 2880×1800 trimmed to the popover region with a clean menu bar).
- Update copyright year and contact details.

## Local preview

```bash
cd website
python3 -m http.server 8000
# open http://localhost:8000
```
