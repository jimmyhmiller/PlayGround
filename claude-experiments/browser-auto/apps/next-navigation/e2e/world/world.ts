import { defineWorld } from "../../../../src/world/index.js";
// The Next.js navigation fixture is stateless server-side (routing only).
export default defineWorld({ reset: () => {}, entities: {} });
