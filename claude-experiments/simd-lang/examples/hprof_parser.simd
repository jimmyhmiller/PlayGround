-- HPROF parser for repacked heap dumps (id_size=8).

fn read_u32_be(buf: ptr[u8], off: i64[1]) -> i64[1] {
    b0 = to_i64(load_at(buf, off))
    b1 = to_i64(load_at(buf, off + 1))
    b2 = to_i64(load_at(buf, off + 2))
    b3 = to_i64(load_at(buf, off + 3))
    return (b0 << 24) | (b1 << 16) | (b2 << 8) | b3
}

fn read_u64_be(buf: ptr[u8], off: i64[1]) -> i64[1] {
    return bswap(load_at[u64[1]](buf, off))
}

-- Build a segment directory in one pass over all records.
-- For each HEAP_DUMP_SEGMENT with an INSTANCE_DUMP first sub-record, stores:
--   dir_class_ids[i]  = class_object_id from first sub-record
--   dir_offsets[i]    = byte offset of segment body within buf
--   dir_lengths[i]    = segment body_length
--   dir_strides[i]    = stride (25 + num_bytes), or 0 for non-instance segments
-- out[0] = number of instance segments found
fn build_directory(buf: ptr[u8], buf_len: i64[1], dir_class_ids: ptr[i64], dir_offsets: ptr[i64], dir_lengths: ptr[i64], dir_strides: ptr[i64], out: ptr[i64]) {
    offset: i64[1] = 31
    idx: i64[1] = 0

    while offset + 50 <= buf_len {
        tag = to_i64(load_at(buf, offset))
        body_len = read_u32_be(buf, offset + 5)

        -- Always read segment fields (safe: offset+34 < offset+50 <= buf_len)
        sub_tag = to_i64(load_at(buf, offset + 9))
        seg_class_id = read_u64_be(buf, offset + 22)
        seg_num_bytes = read_u32_be(buf, offset + 30)
        stride = 25 + seg_num_bytes

        is_instance_seg = (tag == 28) & (sub_tag == 33) & (body_len > 25)

        if is_instance_seg {
            store_at(dir_class_ids, idx, seg_class_id)
            store_at(dir_offsets, idx, offset + 9)
            store_at(dir_lengths, idx, body_len)
            store_at(dir_strides, idx, stride)
            idx = idx + 1
        } else {
            idx = idx
        }

        offset = offset + 9 + body_len
    }

    i0: i32[1] = 0
    store_at(out, i0, idx)
}

-- Single-pass: find class by name and count instances.
-- Kept for convenience when you don't need the directory.
fn find_and_count(buf: ptr[u8], buf_len: i64[1], name_w1: i64[1], name_w2: i64[1], name_len: i64[1], out: ptr[i64]) {
    offset: i64[1] = 31
    target_sid: i64[1] = 0
    target_cid: i64[1] = 0
    count: i64[1] = 0
    matching_segs: i64[1] = 0

    while offset + 50 <= buf_len {
        tag = to_i64(load_at(buf, offset))
        body_len = read_u32_be(buf, offset + 5)

        string_id = read_u64_be(buf, offset + 9)
        text_len = body_len - 8
        text_w1 = read_u64_be(buf, offset + 17)
        text_w2 = read_u64_be(buf, offset + 25)

        utf8_match = (tag == 1) & (text_len == name_len) & (text_w1 == name_w1) & (text_w2 == name_w2)
        if utf8_match {
            target_sid = string_id
        } else {
            target_sid = target_sid
        }

        class_obj_id = read_u64_be(buf, offset + 13)
        class_name_sid = read_u64_be(buf, offset + 25)

        class_match = (tag == 2) & (class_name_sid == target_sid) & (target_sid > 0)
        if class_match {
            target_cid = class_obj_id
        } else {
            target_cid = target_cid
        }

        sub_tag = to_i64(load_at(buf, offset + 9))
        seg_class_id = read_u64_be(buf, offset + 22)
        seg_num_bytes = read_u32_be(buf, offset + 30)
        stride = 25 + seg_num_bytes
        seg_match = (tag == 28) & (sub_tag == 33) & (seg_class_id == target_cid) & (target_cid > 0) & (stride > 25)

        if seg_match {
            count = count + body_len / stride
            matching_segs = matching_segs + 1
        } else {
            count = count
        }

        offset = offset + 9 + body_len
    }

    i0: i32[1] = 0
    store_at(out, i0, count)
    store_at(out, i0 + 1, matching_segs)
}
