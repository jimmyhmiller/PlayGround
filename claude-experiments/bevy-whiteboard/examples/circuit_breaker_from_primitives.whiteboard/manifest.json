{
  "format_version": 1,
  "canvas": {
    "name": "Circuit Breaker (from primitives)",
    "description": "The CircuitBreaker behavior rebuilt by wiring composable primitives — Switch + Window + Threshold + Delay — instead of using the monolithic CircuitBreaker gadget. Demonstrates that the primitive set is sufficient to compose stateful protocols visually."
  }
}
