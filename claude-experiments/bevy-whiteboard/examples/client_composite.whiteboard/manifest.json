{
  "format_version": 1,
  "canvas": {
    "name": "Client (from primitives)",
    "description": "ClientComposite expanded flat: Tick drives the period, Endpoint converts each kick into req(color) pushing self and counts resp/resp_error, Echo replies popping. Same recipe the compound version uses; unwrapped here so the visual shows the pieces."
  }
}
