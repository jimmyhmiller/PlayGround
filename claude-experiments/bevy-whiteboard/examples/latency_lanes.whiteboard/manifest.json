{
  "format_version": 1,
  "canvas": {
    "name": "Latency lanes",
    "description": "Five parallel Client → Worker pairs with edge latencies of 1ms, 20ms, 100ms, 500ms, and 2s. The packet shape and Worker service time are identical across lanes — only the edge latency changes — so you can see the latency directly as packet travel time on the wire."
  }
}
