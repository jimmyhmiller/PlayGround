{
  "format_version": 1,
  "canvas": {
    "name": "Auto Scaling Group",
    "description": "A Generator drives an AutoScalingGroup whose Scaler spawns and destroys ScalableWorker nodes to track load, behind a buffering load balancer. Drill into the ASG to watch the load balancer fan out to the live fleet as it grows under load and shrinks when idle. Select the ASG to tune its scaling policy, thresholds, and worker bounds."
  }
}
