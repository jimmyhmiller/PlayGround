{
  "format_version": 1,
  "canvas": {
    "name": "Pipeline (from primitives)",
    "description": "Generator → Router → Worker → Sink rebuilt by wiring composable primitives — Tick, Filter, Buffer, Delay, Counter — with two color lanes (red, blue) each served by its own backlog and service delay."
  }
}
