# Broadcasts each incoming packet to every outbound neighbor. The
# atom of fan-out — composes with Filter (per-color fan-out) and
# Stamp (request to many participants).
node FanOut {
    rule fan {
        on packet(p)
        do { emit_each packet(p) to out_neighbors() }
    }
}
