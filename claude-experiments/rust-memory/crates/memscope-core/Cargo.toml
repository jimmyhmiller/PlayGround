[package]
name = "memscope-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Tracking global allocator: live heap table, allocation sites, runtime full/sampled modes."

[dependencies]
memscope-proto = { path = "../memscope-proto", default-features = false }
backtrace = { workspace = true }
