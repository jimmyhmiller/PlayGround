[package]
name = "memscope-mcp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Minimal MCP (Model Context Protocol) stdio server exposing memscope's recording analysis (marks / diff / analyze / query) as tools an AI agent can call."

[[bin]]
name = "memscope-mcp"
path = "src/main.rs"

[dependencies]
serde_json = { workspace = true }
