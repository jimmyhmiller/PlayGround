const std = @import("std");
const vector = @import("vector.zig");
const linked_list = @import("linked_list.zig");
const map = @import("map.zig");

test "collections module tests" {
    try std.testing.expect(true);
}