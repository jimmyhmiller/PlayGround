const std = @import("std");
const reader = @import("reader.zig");
const parser = @import("parser.zig");
const lexer = @import("lexer.zig");

test "frontend module tests" {
    try std.testing.expect(true);
}