(module whenlinton)
; Register the one-armed-if rule:
;   coil lint app.coil --use whenlint-on.coil --fix
; Run AFTER ifcombine-on.coil: `(if a (if b x 0) 0)` is an (and), and turning the
; inner one into a `when` first would hide that.
(import "whenlint.coil" :use *)
(checker lint-one-armed-if)
