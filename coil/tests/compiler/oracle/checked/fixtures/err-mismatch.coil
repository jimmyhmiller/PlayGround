(module errtest)
(import "primitive.coil" :as primitive)
(import "alloc.coil" :as alloc)
(defstruct Pt [(x i64) (y i64)])
(defn main [(w (slice u8))] (-> i64)
  (let [p (alloc/stack Pt)]
    (primitive/store! (primitive/field p x) w)
    0))
