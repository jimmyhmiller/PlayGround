(module inherent_duplicate_imports)
(import "primitive.coil" :as primitive)
(import "inherent-remote.coil" :use [Remote])
(import "inherent-extension.coil")
(import "inherent-extension-duplicate.coil")

(defn main [] (-> i64)
  (remote-mark (primitive/zeroed Remote)))
