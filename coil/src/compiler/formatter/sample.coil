; a comment at top
(module app)
(import "../../stdlib/primitive.coil" :as primitive)

(defn add [(a i64) (b i64)] (-> i64)
  (primitive/iadd a b))   ; trailing comment

(defn main [] (-> i64)
  (let [x 'sym  ; quoted
        s "a \"str\""
        cs c"cstr"]
    (add x (foo [1 2 3] `(a ~b ~@c)))))
