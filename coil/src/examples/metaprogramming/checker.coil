; ── A METAPROGRAM CHECKER, built with today's macro machinery ────────────────
; `check-uaf` is a macro [Code…] -> Code that reads a sequence of forms, finds a
; use-after-free (a `(primitive/free X)` followed by a later reference to X), and VETOES
; compilation via `error`. Otherwise it returns the body unchanged. This is the
; "reject" power of a metaprogram — a program that refuses to compile your program.
; Uses only Code builtins (code-eq/code-nth/…), so it needs no imports.
(module checker)
(import "primitive.coil" :as primitive)

; is form `f` a `(primitive/free …)` call?
(defn head-free? [(f Code)] (-> bool)
  (and (primitive/code-list? f)
       (and (primitive/icmp-gt (primitive/code-count f) 0)
            (primitive/code-eq (primitive/code-nth f 0) `free))))

; does form `f` (anywhere in its tree) reference the symbol node `x`?
(defn refs? [(f Code) (x Code)] (-> bool)
  (if (primitive/code-sym? f) (primitive/code-eq f x)
    (if (primitive/code-list? f) (refs-list? f x 0 (primitive/code-count f)) false)))
(defn refs-list? [(f Code) (x Code) (i i64) (n i64)] (-> bool)
  (if (primitive/icmp-ge i n) false
    (if (refs? (primitive/code-nth f i) x) true (refs-list? f x (primitive/iadd i 1) n))))

; is `x` referenced by any body form at index >= j?
(defn used-after? [(body Code) (x Code) (j i64) (n i64)] (-> bool)
  (if (primitive/icmp-ge j n) false
    (if (refs? (primitive/code-nth body j) x) true (used-after? body x (primitive/iadd j 1) n))))

; is the whole sequence free of use-after-free?
(defn uaf-clean? [(body Code) (i i64) (n i64)] (-> bool)
  (if (primitive/icmp-ge i n) true
    (let [f (primitive/code-nth body i)]
      (if (and (head-free? f)
               (used-after? body (primitive/code-nth f 1) (primitive/iadd i 1) n))
          false
        (uaf-clean? body (primitive/iadd i 1) n)))))

(defn check-uaf [& (body Code)] (-> Code)
  (if (uaf-clean? body 0 (primitive/code-count body))
      `(do ~@body)
    (primitive/error "check-uaf: use after free — a freed pointer is read later")))
