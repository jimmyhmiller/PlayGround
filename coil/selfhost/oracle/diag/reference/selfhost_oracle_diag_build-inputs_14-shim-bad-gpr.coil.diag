error: codegen: shim convention names parameter register 'q0', which is not a general-purpose register on the target architecture (aarch64). Declare the convention per-arch (see examples/per-arch.coil).
