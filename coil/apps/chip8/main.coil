(module app)
(import "../../lib/control.coil" :use *)   ; reach the repo stdlib from a subdir project
(defn main [] (-> i64)
  (let [acc (alloc-stack i64)]
    (do (store! acc 0)
        (for [i 0 7] (store! acc (iadd (load acc) 6)))   ; 6*7 = 42
        (load acc))))
