{
  "name": "projects-list",
  "steps": [
    {
      "cmd": ["issues", "init", "--project", "alpha"]
    },
    {
      "cmd": ["issues", "init", "--project", "beta"]
    },
    {
      "cmd": ["issues", "projects", "--json"],
      "expect_json_contains": [
        { "name": "alpha" },
        { "name": "beta" }
      ]
    }
  ]
}
